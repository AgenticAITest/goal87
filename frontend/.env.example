# Frontend dev vars — copy to .env.local for local development.
# In production these are passed as Docker build args (not an env file).
VITE_SUPABASE_URL=https://YOURREF.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_WORKER_URL=http://localhost:3001
